diff --git a/CMakeLists.txt b/CMakeLists.txt
index c862710..c0faaf2 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -37,8 +37,8 @@ set(PROOT_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/src")
 set(SHM_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/third_party/libandroid-shmem")
 set(TALLOC_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/third_party/talloc")
 
-# 1. Samba talloc shared library
-add_library(talloc SHARED "${TALLOC_ROOT}/talloc.c")
+# 1. Samba talloc library (static for standalone proot)
+add_library(talloc STATIC "${TALLOC_ROOT}/talloc.c")
 target_include_directories(talloc PUBLIC "${TALLOC_ROOT}")
 target_compile_definitions(talloc PRIVATE
     _GNU_SOURCE
@@ -48,23 +48,24 @@ target_compile_definitions(talloc PRIVATE
     TALLOC_BUILD_VERSION_RELEASE=0
 )
 target_compile_options(talloc PRIVATE
+    -fPIC
     -Wno-implicit-function-declaration
     -Wno-error=implicit-function-declaration
     -Wno-incompatible-function-pointer-types
 )
-set_target_properties(talloc PROPERTIES OUTPUT_NAME talloc SOVERSION 2)
 
-# 2. Android Shared Memory emulation
+# 2. Android Shared Memory emulation (static for standalone proot)
 if(ANDROID)
-    add_library(android-shmem SHARED "${SHM_ROOT}/shmem.c")
+    add_library(android-shmem STATIC "${SHM_ROOT}/shmem.c")
     target_include_directories(android-shmem PUBLIC "${SHM_ROOT}")
     target_compile_definitions(android-shmem PRIVATE _PATH_TMP="/data/data/com.linuxdroid/cache/")
     target_compile_options(android-shmem PRIVATE
+        -fPIC
         -Wno-implicit-function-declaration
         -Wno-error=implicit-function-declaration
         -Wno-incompatible-function-pointer-types
     )
-    target_link_libraries(android-shmem PRIVATE log android)
+    target_link_libraries(android-shmem PUBLIC log android)
 endif()
 
 # 3. Native JNI process spawner (Android only)
@@ -192,7 +193,7 @@ if(ANDROID)
         "SHELL:-include string.h"
         "SHELL:-include ${SHM_ROOT}/shm.h"
     )
-    target_link_libraries(proot PRIVATE talloc android-shmem)
+    target_link_libraries(proot PRIVATE talloc android-shmem log android)
 else()
     target_link_libraries(proot PRIVATE talloc)
 endif()
@@ -232,8 +233,6 @@ add_custom_command(TARGET proot_loader-carrier POST_BUILD
 # 9. 16KB ELF page size alignment for Android 15 / modern devices
 if(ANDROID)
     set(ALL_TARGETS
-        talloc
-        android-shmem
         linuxdroidspawn
         pocketspawn
         proot
diff --git a/src/arch.h b/src/arch.h
index 06b7f1b..f28b2ee 100644
--- a/src/arch.h
+++ b/src/arch.h
@@ -193,4 +193,17 @@ typedef unsigned char byte_t;
 
 #endif
 
+static inline word_t normalize_tracee_address(word_t address)
+{
+#if defined(ARCH_ARM64) || defined(__aarch64__)
+	return address & 0x00FFFFFFFFFFFFFFULL;
+#else
+	return address;
+#endif
+}
+
+#ifndef UNTAG_ADDRESS
+#define UNTAG_ADDRESS(addr) normalize_tracee_address((word_t)(addr))
+#endif
+
 #endif /* ARCH_H */
diff --git a/src/execve/aoxp.c b/src/execve/aoxp.c
index b1103a6..3667a91 100644
--- a/src/execve/aoxp.c
+++ b/src/execve/aoxp.c
@@ -316,7 +316,7 @@ int fetch_array_of_xpointers(Tracee *tracee, ArrayOfXPointers **array_, Reg reg,
 		return -ENOMEM;
 	array = *array_;
 
-	address = peek_reg(tracee, CURRENT, reg);
+	address = UNTAG_ADDRESS(peek_reg(tracee, CURRENT, reg));
 
 	for (i = 0; nb_entries != 0 ? i < nb_entries : pointer != 0; i++) {
 		void *tmp = talloc_realloc(array, array->_xpointers, XPointer, i + 1);
@@ -328,7 +328,7 @@ int fetch_array_of_xpointers(Tracee *tracee, ArrayOfXPointers **array_, Reg reg,
 		if (errno != 0)
 			return -errno;
 
-		array->_xpointers[i].remote = pointer;
+		array->_xpointers[i].remote = UNTAG_ADDRESS(pointer);
 		array->_xpointers[i].local = NULL;
 	}
 	array->length = i;
diff --git a/src/ptrace/ptrace.c b/src/ptrace/ptrace.c
index dea477f..2eaed08 100644
--- a/src/ptrace/ptrace.c
+++ b/src/ptrace/ptrace.c
@@ -289,6 +289,7 @@ int translate_ptrace_exit(Tracee *tracee)
 		/* Fall through.  */
 	case PTRACE_PEEKTEXT:
 	case PTRACE_PEEKDATA:
+		address = UNTAG_ADDRESS(address);
 		errno = 0;
 		result = (word_t) ptrace(request, pid, address, NULL);
 		if (errno != 0)
@@ -315,6 +316,7 @@ int translate_ptrace_exit(Tracee *tracee)
 
 	case PTRACE_POKETEXT:
 	case PTRACE_POKEDATA:
+		address = UNTAG_ADDRESS(address);
 		if (is_32on64_mode(ptracer)) {
 			word_t tmp;
 
diff --git a/src/tracee/mem.c b/src/tracee/mem.c
index 925f4bd..16b3052 100644
--- a/src/tracee/mem.c
+++ b/src/tracee/mem.c
@@ -97,6 +97,7 @@ static inline void store_word(void *address, word_t value)
 
 static int ptrace_pokedata_or_via_stub(Tracee *tracee, word_t addr, word_t word)
 {
+	addr = UNTAG_ADDRESS(addr);
 	int status = -1;
 #if HAS_POKEDATA_WORKAROUND
 	static bool pokedata_workaround_needed, pokedata_workaround_checked;
@@ -217,6 +218,7 @@ void mem_prepare_before_first_execve(Tracee *tracee)
  */
 int write_data(Tracee *tracee, word_t dest_tracee, const void *src_tracer, word_t size)
 {
+	dest_tracee = UNTAG_ADDRESS(dest_tracee);
 	word_t *src  = (word_t *)src_tracer;
 	word_t *dest = (word_t *)dest_tracee;
 
@@ -298,6 +300,7 @@ int write_data(Tracee *tracee, word_t dest_tracee, const void *src_tracer, word_
  */
 int writev_data(Tracee *tracee, word_t dest_tracee, const struct iovec *src_tracer, int src_tracer_count)
 {
+	dest_tracee = UNTAG_ADDRESS(dest_tracee);
 	size_t size;
 	int status;
 	int i;
@@ -337,6 +340,7 @@ int writev_data(Tracee *tracee, word_t dest_tracee, const struct iovec *src_trac
  */
 int read_data(const Tracee *tracee, void *dest_tracer, word_t src_tracee, word_t size)
 {
+	src_tracee = UNTAG_ADDRESS(src_tracee);
 	word_t *src  = (word_t *)src_tracee;
 	word_t *dest = (word_t *)dest_tracer;
 
@@ -411,6 +415,7 @@ int read_data(const Tracee *tracee, void *dest_tracer, word_t src_tracee, word_t
  */
 int read_string(const Tracee *tracee, char *dest_tracer, word_t src_tracee, word_t max_size)
 {
+	src_tracee = UNTAG_ADDRESS(src_tracee);
 	word_t *src  = (word_t *)src_tracee;
 	word_t *dest = (word_t *)dest_tracer;
 
@@ -542,6 +547,7 @@ fallback:
  */
 word_t peek_word(const Tracee *tracee, word_t address)
 {
+	address = UNTAG_ADDRESS(address);
 	word_t result = 0;
 
 #if defined(HAVE_PROCESS_VM)
@@ -585,6 +591,7 @@ word_t peek_word(const Tracee *tracee, word_t address)
  */
 void poke_word(const Tracee *tracee, word_t address, word_t value)
 {
+	address = UNTAG_ADDRESS(address);
 	word_t tmp;
 
 #if defined(HAVE_PROCESS_VM)
